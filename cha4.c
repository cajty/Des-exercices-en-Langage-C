#include <stdio.h>
#include <math.h>

int main() {
	float a, b, c, dlta;
	printf("entrez le numéro aX² + bX+ C n");
	printf("enter a:", a);
	scanf("%f", &b);
	printf("enter b:", b);
	scanf("%f", &c);
	printf("enter c:", c);
	scanf("%f", &a);
	printf("voter Equation deuxième degré %fX² + %fX + %f \n");
	//dlta =  (b*b) + (4*a*c);
    printf("%f",sqrt(a));

      /*if ( dlta == 0){
     	printf("numéro %d  \n",a);
      }
      else {
     	printf("numéro %d est impaire \n", a);
      } */
}

